export interface MkOfferResult {
  url: string;
  status: string;
}

export interface SocketResponse {
  error: boolean;
  status: string;
}

export interface CollectionInfo {
  floorPrice: number;
  name: string;
}
